# Experiences plugin

This plugin packages the site's experiences content as a standalone plugin. It's a minimal placeholder used to expose the navigation item and to serve as a template for packaged experiences plugins.

Place content under a `content/` directory or add additional server-specific hooks if desired.
