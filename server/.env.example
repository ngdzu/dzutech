PORT=4000
ALLOWED_ORIGIN=https://dzutech.com,https://www.dzutech.com
DATABASE_URL=postgresql://dzutech:dzutech_password@db:5432/dzutech
# Generate secret with somthing like `openssl rand -hex 32`
SESSION_SECRET=please-change-me
# Optional cookie tuning: auto | true | false (defaults to auto)
# SESSION_COOKIE_SECURE=auto
# SameSite options: strict | lax | none (defaults to strict in production, lax otherwise)
# SESSION_COOKIE_SAMESITE=
ADMIN_EMAIL=admin@example.com
# Generate hash with `npm run hash-password` (after running npm install)
ADMIN_PASSWORD_HASH=