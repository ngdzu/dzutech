PORT=4000
ALLOWED_ORIGIN=https://examplexyz.com,https://www.examplexyz.com

# DATABASE_URL is in this format:
# DATABASE_URL=postgresql://${DB_USER}:${DB_PASSWORD}@db:${DB_PORT}/${DB_NAME}
DATABASE_URL=postgresql://appuser:changeme@db:5432/appdb

# TLS between the API and the database is strongly recommended
# Set DB_SSL=true when your database supports encrypted connections
# DB_SSL=true
# If your provider requires a CA certificate, paste it here (use \n for newlines)
# DB_SSL_CA="-----BEGIN CERTIFICATE-----\n...\n-----END CERTIFICATE-----\n"
# Set to false only if you intentionally allow self-signed certificates
# DB_SSL_REJECT_UNAUTHORIZED=true

# Generate SECRET with something like `openssl rand -hex 32`
SESSION_SECRET=2f82b8a1325eca21dae7a898e03900ee354f6e13f2089e7aad638c90b57d2e4b
# Optional cookie tuning: auto | true | false (defaults to auto)
# SESSION_COOKIE_SECURE=auto
# SameSite options: strict | lax | none (defaults to strict in production, lax otherwise)
# SESSION_COOKIE_SAMESITE=

#this email will be used to login to /admin
ADMIN_EMAIL=admin@example.com
# Generate hash with `npm run hash-password changeme`
# The following password is for changeme. 
ADMIN_PASSWORD_HASH=$$2b$$12$$EECcsnfr5wKsijajPuHc/eoQAHNec/squ/aLBydY8c5fo1HqoblES


# MinIO (S3-compatible) dev settings
S3_ENDPOINT=http://minio:9000
S3_BUCKET=example-uploads
AWS_ACCESS_KEY_ID=minioadmin
AWS_SECRET_ACCESS_KEY=minioadmin
S3_FORCE_PATH_STYLE=true