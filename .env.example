# Frontend build-time configuration
# In prod, it should be https://example.com/api
VITE_API_URL=http://localhost:4000/api

# Docker Compose defaults (override as needed)
DB_NAME=appdb
DB_USER=appuser
DB_PASSWORD=changeme
DB_PORT=5432

# for production, this list should contain the real domain
ALLOWED_ORIGIN=http://localhost:4173

# Generate secret with somthing like `openssl rand -hex 32`
SESSION_SECRET=2f82b8a1325eca21dae7a898e03900ee354f6e13f2089e7aad638c90b57d2e4b

# uncomment the following line if not running locally (not in a container)
DATABASE_URL=postgresql://appuser:changeme@db:5432/appdb

# Optional cookie tuning: auto | true | false (defaults to auto)
# SESSION_COOKIE_SECURE=auto
# SameSite options: strict | lax | none (defaults to strict in production, lax otherwise)
# SESSION_COOKIE_SAMESITE=

# for local development, leave them empty. 
CADDY_EMAIL=""
CADDY_DOMAINS=""

# for production, these need to be valid values
# CADDY_EMAIL="admin@example.com"
# CADDY_DOMAINS="example.com"